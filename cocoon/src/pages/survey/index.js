import React from 'react';
import { Component } from 'react';

export default class Survey extends Component {
    render(){
        return (
            <h1>Rendering the survey here!</h1>
        );
    }
}